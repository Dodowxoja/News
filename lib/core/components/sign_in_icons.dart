import 'package:flutter/material.dart';

class SignInIcons {
  static Container signInIcons() {
    return Container(
      height: 40,
      width: 43,
      decoration: BoxDecoration(
        border: Border.all(color: const Color(0xff180E19), width: 1.5),
        borderRadius: BorderRadius.circular(5),
      ),
    );
  }
}
